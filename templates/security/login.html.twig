<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>{% block title %}Se connecter{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
        <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;1,300&display=swap" rel="stylesheet">
    {% endblock %}
</head>

<body>
        <div class="bodylogin">
            <a href="{{ path('index_map') }}"><img class="logologin" src="{{ asset('/build/images/logo-login.png') }}"
            alt="Logo Sorties Nature"></a>
            {% for category, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ category }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}

            <form method="post">

                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-3">
                        You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                {% endif %}

                <div class="connecter">
                    <div class="titrelogin">
                        <h1 class="h3 mb-3 font-weight-normal">Se connecter</h1>
                    </div>
                    <div class="inscrirelogin">
                        <p>Nouvel utilisateur ? <a href="{{ path('app_register') }}">S'inscrire</a></p>
                    </div>
                  <div class="loginborder">
                    <div class="loginmail">
                        <label for="inputMail">Email</label>
                        <input type="text" value="{{ last_username }}" name="mail" id="inputMail" class="form-control" required autofocus>
                    </div>
                    <div class="mdplogin">
                        <label for="inputPassword">Mot de Passe</label>
                        <input type="password" name="password" id="inputPassword" class="form-control" required>
                    </div>
                      <input type="hidden" name="_csrf_token"
                           value="{{ csrf_token('authenticate') }}"
                    >


                {#        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.#}
                {#        See https://symfony.com/doc/current/security/remember_me.html#}

                        <div class="checkbox mb-3 remember">
                            <label>
                                <input type="checkbox" name="_remember_me"> Se souvenir de moi
                            </label>
                            <label>
                                <input type="checkbox" name="_mdp_oublie"> Mot de passe oublié ?
                            </label>
                        </div>
                        <div class="buttonlogin">
                            <button class="btn btn-lg btn-primary" type="submit">
                                Se connecter
                            </button>
                        </div>
                  </div>

                </div>

    <div class="etoile">
    <p>* En vous inscrivant, vous acceptez nos Conditions d’utilisation et de recevoir des e-mails et vous reconnaissez
        avoir lu notre Politique de confidentialité. Vous acceptez également l’utilisation des cookies pour vous offrir
        un meilleur service.</p>
    </div>
</form>
        </div>
</body>
